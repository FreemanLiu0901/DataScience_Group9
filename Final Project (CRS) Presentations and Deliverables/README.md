
# Healthcare Chatbot Recommender System Project

This notebook implements a healthcare chatbot recommender system. It involves data preparation, feature engineering, clustering analysis (Person 1's work), and building and evaluating collaborative filtering recommender algorithms (Person 2's work), followed by integration and reporting.

## Prerequisites

To run this notebook, you need a Python environment, preferably with Google Colab or similar. The notebook requires the following libraries:

- `surprise`
- `pandas`
- `numpy`
- `scikit-learn`
- `matplotlib`
- `seaborn`
- `nltk`
- `joblib`

## Setup and Installation

1.  **Open the notebook** in Google Colab or your preferred environment.
2.  **Install Libraries:** Run the cell(s) that install the required libraries. Look for cells starting with `!pip install ...`. Ensure compatible versions are installed, especially for `numpy` if dependency conflicts arise (refer to the notebook's installation cells for specific version handling like `numpy==1.23.5`).
3.  **Download NLTK Data:** Run the cell that downloads necessary NLTK data (`nltk.download(...)`). This includes 'punkt', 'stopwords', 'wordnet', 'omw-1.4', 'averaged_perceptron_tagger', and 'punkt_tab'.

## Required Data Files

This notebook expects the following generated or input data files:

- `healthcare_qa_processed.csv`: The main processed dataset used for both clustering and the recommender system. This is generated in the Data Preparation step (Person 1).
- `vectors_*.pkl`: Pickled files containing feature vectors generated during Feature Engineering (Person 1).
- `clustering_labels_*.npy`: Numpy files containing clustering labels generated during Clustering Analysis (Person 1).
- `clustering_visualizations/error_analysis_by_intent.csv`: CSV file with clustering error analysis results (Person 1).
- `final_project_summary.json`: A JSON file summarizing key results from Person 1's work.

Ensure these files are generated by running the relevant steps in the notebook before proceeding to dependent sections.

## Execution Order

Run the cells in the notebook sequentially, following the section titles and comments. The key steps correspond to the sections for Person 1 and Person 2:

1.  **Initial Setup and Imports:** Run the initial cells to import libraries and define helper functions/classes.
2.  **NLTK Data Download:** Run the cell to download NLTK resources.
3.  **Data Preparation & Feature Engineering (Person 1):** Run the cells related to loading, processing, and creating features from the healthcare data. This generates `healthcare_qa_processed.csv` and `vectors_*.pkl`.
4.  **Clustering Analysis (Person 1):** Run the cells that apply clustering algorithms, evaluate them, generate visualizations, and perform error analysis. This generates clustering label files, visualization files, and `clustering_visualizations/error_analysis_by_intent.csv`.
5.  **Save Summary (Person 1):** Run the cell that saves the summary of Person 1's work to `final_project_summary.json`.
6.  **Recommender Engine (Person 2):** Run the cell(s) that build and evaluate the recommender system models (`kr-ZFU7B9rJg`). This step reads `healthcare_qa_processed.csv` and generates evaluation metrics and visualizations.
7.  **Final Summary and Handover:** Run the cell that generates the final report (`1d77f342c93c8c1b`). This cell reads the existing markdown report and appends the recommender system evaluation summary to it.

Follow any specific instructions or troubleshooting steps mentioned in the notebook cells (e.g., regarding library version conflicts or file paths).

## Output Files

Running the notebook will generate various output files, including:

- `healthcare_qa_processed.csv`
- `vectors_*.pkl`
- `clustering_labels_*.npy`
- `clustering_visualizations/*.png`, `*.csv` (plots and analysis from clustering)
- `final_project_summary.json`
- `recommender_evaluation_extended.png` (Aggregate recommender metrics plot)
- `recommender_per_intent_evaluation.png` (Per-intent recommender metrics plot)
- `recommender_predicted_scores_sample_intents.png` (Sample predicted scores plot)
- `final_project_report.md`: The final integrated report.
- `README.md`: This file.

